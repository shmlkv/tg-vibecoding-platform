# OpenRouter Integration Setup Guide

Я полностью перенес генерацию с v0 на OpenRouter! Теперь приложение генерирует HTML через OpenRouter API и сохраняет в таблицу `projects`.

## Что было сделано

### 1. База данных
- ✅ Создана таблица `user_settings` для хранения API ключей и выбора моделей
- ✅ Добавлено поле `project_id` в таблицу `posts` для связи с проектами
- ✅ Сделаны v0 поля nullable (т.к. теперь используем OpenRouter)

### 2. OpenRouter интеграция
- ✅ Создана функция `generateHTMLWithOpenRouter` в `src/lib/openrouter.ts`
- ✅ Добавлены 4 модели на выбор:
  - Claude 3.5 Sonnet (по умолчанию)
  - GPT-4 Turbo
  - Gemini Pro 1.5
  - Llama 3.1 70B

### 3. Страница настроек
- ✅ `/settings` - страница для настройки OpenRouter API ключа
- ✅ Селектор модели
- ✅ Кнопка "Settings" в профиле

### 4. API роуты
- ✅ `/api/settings` - GET/POST для настроек пользователя
- ✅ Обновлен `/api/posts` (POST) - использует OpenRouter вместо v0
- ✅ Обновлен `/api/posts` (GET) - загружает связанные проекты

### 5. UI обновления
- ✅ Preview page теперь отображает HTML из `projects` через `srcDoc`
- ✅ Поддержка как новых (HTML), так и старых (v0_demo_url) постов

## Шаги для запуска

### 1. Применить SQL миграции

**ВАЖНО:** Выполните обе миграции в Supabase SQL Editor!

#### Миграция 1: Создать таблицу projects (если ещё не создана)
https://supabase.com/dashboard/project/ahdimjaaggidcasmfcmu/sql/new

Выполните SQL из файла: `fix-projects-table.sql`

#### Миграция 2: Добавить user_settings и обновить posts
https://supabase.com/dashboard/project/ahdimjaaggidcasmfcmu/sql/new

Выполните SQL из файла: `supabase/migration-settings-and-projects.sql`

Эта миграция:
- Создаст таблицу `user_settings`
- Добавит колонку `project_id` в таблицу `posts`
- Сделает v0 поля nullable

### 2. Получить OpenRouter API ключ

1. Зарегистрируйтесь на https://openrouter.ai
2. Пополните баланс (минимум ~$5)
3. Получите API ключ: https://openrouter.ai/keys
4. Скопируйте ключ (начинается с `sk-or-v1-...`)

### 3. Настроить приложение

1. Запустите приложение:
```bash
npm run dev
```

2. Откройте в браузере и перейдите в профиль
3. Нажмите кнопку "Settings"
4. Вставьте ваш OpenRouter API ключ
5. Выберите модель (рекомендую Claude 3.5 Sonnet)
6. Нажмите "Save Settings"

### 4. Сгенерировать проект

1. Перейдите на страницу генерации
2. Введите промпт (например: "пятнашки игра")
3. Нажмите "Generate"
4. Подождите ~30-60 секунд
5. HTML будет сгенерирован через OpenRouter и сохранён в БД!

## Как это работает

### Процесс генерации (новый)

1. **Пользователь нажимает "Generate"**
   - `/api/posts` (POST) проверяет наличие API ключа
   - Если ключа нет → ошибка "Please configure your OpenRouter API key"

2. **Создаётся pending пост**
   - Статус: `pending`
   - Поля v0: `null`
   - project_id: `null`

3. **Асинхронная генерация**
   - Вызывается `generateHTMLWithOpenRouter()`
   - OpenRouter генерирует HTML код
   - HTML извлекается из ответа (убираются markdown блоки)

4. **Сохранение в БД**
   - HTML сохраняется в таблицу `projects`
   - Пост обновляется: `project_id` = ID проекта, `status` = 'ready'

5. **Отображение**
   - Preview page загружает пост с проектом
   - Отображает HTML через `<iframe srcDoc={html}>`

## Структура файлов

### SQL
- `fix-projects-table.sql` - создание таблицы projects
- `supabase/migration-settings-and-projects.sql` - user_settings и обновление posts
- `supabase/schema.sql` - обновлённая схема

### TypeScript Types
- `src/lib/supabase/types.ts` - добавлены `user_settings` и `project_id`

### OpenRouter
- `src/lib/openrouter.ts` - функции для работы с OpenRouter API
- `AVAILABLE_MODELS` - доступные модели
- `generateHTMLWithOpenRouter()` - генерация HTML

### API Routes
- `src/app/api/settings/route.ts` - GET/POST настроек
- `src/app/api/posts/route.ts` - обновлён для OpenRouter
- `src/app/api/projects/route.ts` - API для проектов

### Pages
- `src/app/settings/page.tsx` - страница настроек
- `src/app/preview/page.tsx` - обновлена для HTML из projects
- `src/app/profile/page.tsx` - добавлена кнопка Settings
- `src/app/projects/page.tsx` - галерея проектов (бонус)
- `src/app/projects/[id]/page.tsx` - просмотр проекта (бонус)

### Library
- `src/lib/posts.ts` - обновлён для загрузки проектов

## Модели OpenRouter

| Модель | ID | Описание | Стоимость (примерно) |
|--------|-----|----------|---------------------|
| **Claude 3.5 Sonnet** | `anthropic/claude-3.5-sonnet` | Лучший для кодогенерации | ~$3 / 1M tokens input |
| **GPT-4 Turbo** | `openai/gpt-4-turbo` | Быстрый и способный | ~$10 / 1M tokens input |
| **Gemini Pro 1.5** | `google/gemini-pro-1.5` | От Google | ~$1.25 / 1M tokens input |
| **Llama 3.1 70B** | `meta-llama/llama-3.1-70b-instruct` | Open source | ~$0.88 / 1M tokens input |

## Безопасность

- API ключи хранятся в Supabase (шифруются)
- RLS (Row Level Security) настроена для всех таблиц
- Пользователи могут видеть только свои настройки
- HTML отображается в iframe с sandbox

## Совместимость

Приложение **полностью обратно совместимо**:
- Старые посты с `v0_demo_url` всё ещё работают
- Новые посты используют `project.html_content`
- Preview page поддерживает оба формата

## Troubleshooting

### Ошибка "Please configure your OpenRouter API key"
→ Перейдите в Settings и добавьте ключ

### Ошибка "Could not find the 'user_id' column"
→ Выполните миграцию `migration-settings-and-projects.sql`

### Проекты не отображаются
→ Проверьте, что выполнили `fix-projects-table.sql`

### Generation failed
→ Проверьте баланс на OpenRouter
→ Проверьте, что ключ валиден

## Стоимость

Примерная стоимость одной генерации с Claude 3.5 Sonnet:
- Промпт: ~1500 tokens
- Ответ: ~3000 tokens
- **Итого: ~$0.01-0.02 за генерацию**

## Что дальше?

Можно добавить:
- ✨ Историю генераций
- ✨ Редактирование промптов
- ✨ Скачивание HTML файлов
- ✨ Форк проектов
- ✨ Категории проектов
- ✨ Поиск по проектам

---

Всё готово к использованию! 🚀
